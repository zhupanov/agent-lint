# Project instructions

password = $TOKEN
api_key = ${API_KEY}
secret = ${SECRET:-}
access_key = {{ACCESS_KEY}}
client_secret = <CLIENT_SECRET>
private_key =

Run `cargo test` and keep docs/README.md current.
