# Local agent-lint installer

This directory contains an installer script, not an invocable skill. From the
repository root, install or upgrade the latest published release in a terminal:

```bash
sudo .claude/skills/upgrade-agent-lint/scripts/upgrade-agent-lint.sh
```
